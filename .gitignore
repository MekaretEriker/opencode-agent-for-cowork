# Build outputs
*.plugin
*.zip

# Dev artifacts
node_modules/
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Local memory (user workspace files, never committed at plugin level)
.opencode-agent-memory.json
.opencode-agent-traces/
*.plugin

# Operator-specific OpenRouter roster — see AGENTS.md "OpenRouter workspace"
# for the rationale (model list is tied to one operator's workspace and
# should not be versioned across deployments). Sample sections of the format
# are documented in AGENTS.md itself.
OPENROUTER-MODELS.md

# Release helper scripts — generated per release as templates, not source
release-v*.sh
release-v*.log
