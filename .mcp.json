{
  "mcpServers": {
    "opencode": {
      "command": "npx",
      "args": ["-y", "opencode-mcp@^1.10.1"],
      "env": {
        "OPENCODE_DEFAULT_PROVIDER": "${OPENCODE_DEFAULT_PROVIDER}"
      }
    }
  }
}
